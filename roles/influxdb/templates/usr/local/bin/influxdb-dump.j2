#!/usr/bin/env bash
# 2022050901

BACKUP_DIR='/backup/influxdb-dump'

mkdir -p "$BACKUP_DIR"

# detect paths
INFLUXD="$(which influxd)"

# Delete the former dump
echo "Delete former dump files."
rm -f "$BACKUP_DIR/*"

# full dump
"$INFLUXD" backup -portable "$BACKUP_DIR"
echo "Fulldump finished."
