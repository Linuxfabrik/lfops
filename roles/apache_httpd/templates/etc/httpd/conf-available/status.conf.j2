# {{ ansible_managed }}
# 2021110301
{% if item.value.by_role|d() %}
# Generated by Ansible role: {{ item.value.by_role }}
{% endif %}

# mod_status
# Keep track of extended status information for each request
ExtendedStatus On
# The alias prevents the processing of .htaccess files, which could contain RewriteRules
# that interfere with our rule.
Alias /server-status /dev/null
<Location /server-status>
    SetHandler server-status
    Require local
    # OpenVPN net1 network
    Require ip 10.199.26
</Location>

# Determine if mod_status displays the first 63 characters of a request or
# the last 63, assuming the request itself is greater than 63 chars.
# Default: Off
#SeeRequestTail On

# Show Proxy LoadBalancer status in mod_status
ProxyStatus On

{{ item.value.raw|d() }}
