# {{ ansible_managed }}
# 2022100502
{% if item['by_role'] | d() %}
# Generated by Ansible role: {{ item['by_role'] }}
{% endif %}

# headers_module>
# Global header definition for all vHosts

# Security Header: leaks information about the server side technology and thus helps attackers picking exploits from their toolbox
Header always unset X-Powered-By

# Security Header: HTTP Strict Transport Security (HSTS) enforces the use of TLS
# Do not use "always" here, because otherwise you migth get "There was a duplicate Strict-Transport-Security header."
# with some applications.
Header set Strict-Transport-Security: "max-age=63072000; includeSubDomains; preload"

# Security Header: Feature Policy has been renamed to Permissions Policy
Header always unset Feature-Policy

# Security Header: Permissions Policy is a new header that allows a site to control which features and APIs can be used in the browser.
Header always set Permissions-Policy: "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()"

