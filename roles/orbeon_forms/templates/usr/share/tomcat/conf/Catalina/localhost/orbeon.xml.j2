<!-- {{ ansible_managed }} -->
<!-- Numishare specific configuration -->
<!--
  When this descriptor lives at Catalina/localhost/<name>.xml the context path is
  derived from the file name; setting `path` here is redundant and triggers a
  startup warning, so it is omitted on purpose. The file is named after the webapp
  directory (orbeon.xml -> context path "/orbeon"). The reverse proxy in front maps
  the clean public URLs (/id/..., /themes/..., /admin/, /) onto this /orbeon backend.
-->
<Context><Resources allowLinking="true"/></Context>
