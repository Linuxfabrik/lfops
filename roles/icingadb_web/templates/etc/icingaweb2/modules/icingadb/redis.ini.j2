; {{ ansible_managed }}
; 2023040601

[redis1]
host = "{{ icingadb_web__redis_host }}"
port = {{ icingadb_web__redis_port }}
{% if icingadb_web__redis_password is defined and icingadb_web__redis_password | length %}
password = "{{ icingadb_web__redis_password }}"
{% endif %}
