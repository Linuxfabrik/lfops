# skip restart if the operator deferred restarts LFOps-wide (lfops__skip_restart_handlers), if
# the service was just started (redundant), or if the user wants it stopped.
# __kibana__service_state_result is registered on a dedicated state-only task (not the enabled
# task), so that enabled-only changes don't prevent the restart.
# `is not defined` covers a run that skips the state block (`--skip-tags kibana:state`), where
# the result is never registered.
- name: 'kibana: restart kibana'
  ansible.builtin.service:
    name: 'kibana'
    state: 'restarted'
  when:
    - 'not (lfops__skip_restart_handlers | d(false) | bool)'
    - '__kibana__service_state_result is not defined or __kibana__service_state_result is not changed'
    - 'kibana__service_state != "stopped"'
