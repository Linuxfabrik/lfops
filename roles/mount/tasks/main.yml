- name: 'Set platform/version specific variables'
  ansible.builtin.import_role:
    name: 'shared'
    tasks_from: 'platform-variables.yml'
  tags:
    - 'mount'


- block:

  - name: 'Install {{ mount__mount_package }}'
    ansible.builtin.package:
      name: '{{ mount__mount_package }}'
      state: 'present'

  tags:
    - 'mount'


- block:

  - name: 'mkdir -p nfs-mount-point'
    ansible.builtin.file:
      path: '{{ item["path"] }}'
      state: 'directory'
      owner: 'root'
      group: 'root'
      mode: 0o755
    loop: '{{ mount__mounts }}'

  - name: 'Mount NFS volumes'
    ansible.posix.mount:
      src: '{{ item["src"] }}'
      path: '{{ item["path"] }}'
      opts: '{{ item["opts"] }}'
      state: '{{ item["state"] }}'
      fstype: 'nfs'
    loop: '{{ mount__mounts }}'

  when:
    - 'mount__mounts is defined'
    - 'mount__mounts | length'
  tags:
    - 'mount'
