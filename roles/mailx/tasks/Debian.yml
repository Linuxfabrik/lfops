- block:

  - name: 'Be sure mailx is installed'
    ansible.builtin.package:
      name: 'mailx'
      state: 'present'

  - name: 'Be sure mailx is installed'
    ansible.builtin.package:
      name: 'bsd-mailx'
      state: 'present'

  tags:
    - 'mailx'
