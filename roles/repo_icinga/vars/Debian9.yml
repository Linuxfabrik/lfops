repo_icinga__repo_urls:
  - 'deb http://packages.icinga.com/{{ ansible_facts["distribution"] | lower }} icinga-{{ ansible_facts["distribution_release"] }} main'
  - 'deb-src http://packages.icinga.com/{{ ansible_facts["distribution"] | lower }} icinga-{{ ansible_facts["distribution_release"] }} main'
  - 'deb https://deb.debian.org/debian {{ ansible_facts["distribution_release"] }}-backports main'
