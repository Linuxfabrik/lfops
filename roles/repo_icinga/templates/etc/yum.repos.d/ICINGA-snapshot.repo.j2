# {{ ansible_managed }}
# 2022040501

[icinga-snapshot-builds]
name=ICINGA (snapshot builds for epel)
{% if repo_icinga__mirror_url is defined and repo_icinga__mirror_url|length %}
baseurl={{ repo_icinga__mirror_url }}/icinga/epel/$releasever/snapshot/
{% else %}
baseurl=http://packages.icinga.com/epel/$releasever/snapshot/
{% endif %}
enabled=0
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-ICINGA
metadata_expire=1d
