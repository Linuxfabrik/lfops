# skip restart if the service was just started (redundant) or if the user wants it stopped.
# __docker__service_state_result is registered on a dedicated state-only task (not the enabled
# task), so that enabled-only changes don't prevent the restart.
# `is not defined` covers a run that skips the state block (`--skip-tags docker:state`), where
# the result is never registered.
- name: 'docker: restart docker'
  ansible.builtin.service:
    name: 'docker'
    state: 'restarted'
  when:
    - '__docker__service_state_result is not defined or __docker__service_state_result is not changed'
    - 'docker__service_state != "stopped"'
