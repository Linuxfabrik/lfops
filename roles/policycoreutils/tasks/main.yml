- block:

  - name: 'Set platform/version specific variables'
    ansible.builtin.import_role:
      name: 'shared'
      tasks_from: 'platform-variables.yml'

  - name: 'Make sure SELinux {{ __policycoreutils__package }} is installed'
    ansible.builtin.package:
      name: '{{ __policycoreutils__package }}'
      state: 'present'

  tags:
    - 'policycoreutils'
