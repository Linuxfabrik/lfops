- name: 'Set platform/version specific variables'
  ansible.builtin.import_role:
    name: 'shared'
    tasks_from: 'platform-variables.yml'
  tags:
    - 'always'


- block:

  - name: 'Install package {{ java__package }}'
    ansible.builtin.package:
      name: '{{ java__package }}'
      state: 'present'

  tags:
    - 'java'
