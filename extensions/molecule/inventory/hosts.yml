# yamllint disable rule:empty-values

# Required for this directory to be an Ansible inventory source

all:
  hosts:

    # The libvirt hypervisor the VM backend provisions on. Its variables live in
    # host_vars/hypervisor.yml, like those of every other host in this inventory.
    hypervisor:
