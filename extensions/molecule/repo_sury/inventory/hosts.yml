# yamllint disable rule:empty-values

# repo_sury targets 'lfops_repo_sury' (see playbooks/repo_sury.yml: hosts). The
# sury repo file uses the Debian codename (distribution_release) and
# packages.sury.org only ships Debian suites, so only Debian hosts run.
lfops_repo_sury:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    debian12-vm:
    debian13-vm:
