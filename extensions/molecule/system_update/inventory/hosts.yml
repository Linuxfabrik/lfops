# yamllint disable rule:empty-values

# Map the playbook's target group (playbooks/system_update.yml: hosts:
# lfops_system_update) onto the shared systems_under_test host set. Trim this
# (or use LFOPS_TEST_TARGETS at runtime) to test against fewer hosts.
lfops_system_update:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    debian11-vm:
    debian12-vm:
    debian13-vm:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
    ubuntu2004-vm:
    ubuntu2204-vm:
    ubuntu2404-vm:
    ubuntu2604-vm:
