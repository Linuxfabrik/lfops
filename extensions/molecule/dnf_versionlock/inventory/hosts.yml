# yamllint disable rule:empty-values
lfops_dnf_versionlock:
  children:
    systems_under_test:

# the role drives the dnf versionlock plugin, so only the dnf platforms apply
systems_under_test:
  hosts:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
