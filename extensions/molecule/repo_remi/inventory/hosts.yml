# yamllint disable rule:empty-values

# repo_remi targets 'lfops_repo_remi' (see playbooks/repo_remi.yml: hosts). The
# role is RedHat-only (it deploys dnf module streams), so only RedHat hosts run.
lfops_repo_remi:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
