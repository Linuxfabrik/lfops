# yamllint disable rule:empty-values

# php__version stays unset on both families, as on a host that follows the role's defaults, so the
# role has to discover the version from the installed packages instead of taking the declared one.
lfops_php:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    debian12-vm:
    debian13-vm:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
