# yamllint disable rule:empty-values

# php targets 'lfops_php' (see playbooks/php.yml: hosts). Only Debian hosts run here: this scenario
# covers the Debian-only version handling (php__version, versioned packages, update-alternatives).
# On RedHat the version is pinned at repo level by the module stream, so there is nothing
# equivalent to verify there.
lfops_php:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    debian12-vm:
    debian13-vm:
