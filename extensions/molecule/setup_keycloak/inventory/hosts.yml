# yamllint disable rule:empty-values

# setup_keycloak targets 'lfops_setup_keycloak' (see playbooks/setup_keycloak.yml: hosts).
# Only Red Hat-family hosts run: the keycloak role writes its environment file to
# /etc/sysconfig and installs the java-*-openjdk-headless packages, both of which are
# Red Hat-only. keycloak is x/(x) on RHEL in COMPATIBILITY.md and blank for Debian and
# Ubuntu, which matches.
lfops_setup_keycloak:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
