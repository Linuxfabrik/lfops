# yamllint disable rule:empty-values

# setup_wordpress targets 'lfops_setup_wordpress' (see playbooks/setup_wordpress.yml: hosts).
# Only Red Hat-family hosts run: the playbook installs PHP from Remi (repo_remi, RedHat-gated), and
# the wordpress role hard-codes the apache user and SELinux file contexts. COMPATIBILITY.md lists
# wordpress on RHEL 8, 9 and 10 only.
lfops_setup_wordpress:
  children:
    systems_under_test:

systems_under_test:
  hosts:
    rocky8-vm:
    rocky9-vm:
    rocky10-vm:
