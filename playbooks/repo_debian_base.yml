- name: 'Playbook linuxfabrik.lfops.repo_debian_base'
  hosts:
    - 'lfops_repo_debian_base'

  pre_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-start.yml'


  roles:

    - role: 'linuxfabrik.lfops.repo_debian_base'


  post_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-end.yml'
