- name: 'Installs and configures fail2ban'
  collections: ['linuxfabrik.lfops']
  hosts:
    - 'lfops_fail2ban'

  roles:

    - role: 'policycoreutils'
      when:
        - 'ansible_facts["os_family"] == "RedHat"'

    - role: 'linuxfabrik.lfops.sshd'
      sshd__dependent_log_level: 'VERBOSE'
      when:
        - 'not fail2ban__skip_sshd | d(false)'

    - role: 'repo_epel'
      when:
        - 'ansible_facts["os_family"] == "RedHat"'
        - 'ansible_facts["distribution_major_version"] in ["7", "8"]'
        - 'not fail2ban__skip_repo_epel | d(false)'

    - role: 'fail2ban'
