- name: 'Installs and manages the Collabora CODE'
  hosts:
    - 'lfops_collabora_code'

  roles:

    - role: 'linuxfabrik.lfops.repo_collabora_code'
      when:
        - 'not collabora_code__skip_repo_collabora_code | default(false)'

    - role: 'linuxfabrik.lfops.collabora_code'
