- name: 'Manages the Service DNF-Makecache on the Servers'
  collections: ['linuxfabrik.lfops']
  hosts:
    - 'lfops_dnf-makecache'

  roles:

    - role: 'dnf-makecache'
      when:
        - ansible_facts['os_family'] == 'RedHat' and ansible_facts['distribution_major_version'] in ['8']
          or ansible_facts['distribution'] == 'Fedora'

