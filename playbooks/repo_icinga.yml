- name: 'Playbook linuxfabrik.lfops.repo_icinga'
  hosts:
    - 'lfops_repo_icinga'

  pre_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-start.yml'


  roles:

    - role: 'linuxfabrik.lfops.repo_icinga'


  post_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-end.yml'
