- name: 'Manage and configure SNMP'
  hosts:
    - 'lfops_snmp'

  roles:

    - role: 'linuxfabrik.lfops.snmp'
      when:
        - 'ansible_facts["os_family"] == "RedHat"'
        - 'not snmp__skip_snmp | default(false)'
