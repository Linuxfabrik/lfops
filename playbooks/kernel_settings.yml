- name: 'Playbook linuxfabrik.lfops.kernel_settings'
  hosts:
    - 'lfops_kernel_settings'

  pre_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-start.yml'


  roles:

    - role: 'linuxfabrik.lfops.kernel_settings'


  post_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-end.yml'
