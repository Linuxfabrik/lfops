- name: 'Playbook linuxfabrik.lfops.repo_gitlab_runner'
  hosts:
    - 'lfops_repo_gitlab_runner'

  pre_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-start.yml'


  roles:

    - role: 'linuxfabrik.lfops.repo_gitlab_runner'


  post_tasks:
    - ansible.builtin.import_role:
        name: 'shared'
        tasks_from: 'log-end.yml'
